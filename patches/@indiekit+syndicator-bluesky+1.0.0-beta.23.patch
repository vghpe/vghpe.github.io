diff --git a/node_modules/@indiekit/syndicator-bluesky/lib/bluesky.js b/node_modules/@indiekit/syndicator-bluesky/lib/bluesky.js
index c754a8f..cb991bd 100644
--- a/node_modules/@indiekit/syndicator-bluesky/lib/bluesky.js
+++ b/node_modules/@indiekit/syndicator-bluesky/lib/bluesky.js
@@ -1,3 +1,4 @@
+import process from "node:process";
 import { AtpAgent } from "@atproto/api";
 import { IndiekitError } from "@indiekit/error";
 import { getCanonicalUrl, isSameOrigin } from "@indiekit/util";
@@ -176,7 +177,24 @@ export class Bluesky {
 
     try {
       const mediaUrl = getCanonicalUrl(url, me);
-      const mediaResponse = await fetch(mediaUrl);
+      let mediaResponse = await fetch(mediaUrl);
+
+      // If the public URL 404s (e.g. GitHub Pages hasn't rebuilt yet),
+      // fall back to the raw GitHub content URL
+      if (!mediaResponse.ok && mediaResponse.status === 404) {
+        const ghUser = process.env.GITHUB_USER;
+        const ghRepo = process.env.GITHUB_REPO;
+        const ghBranch = process.env.GITHUB_BRANCH || 'main';
+
+        if (ghUser && ghRepo) {
+          const relativePath = url.startsWith('http')
+            ? new URL(url).pathname.replace(/^\//, '')
+            : url;
+          const rawUrl = `https://raw.githubusercontent.com/${ghUser}/${ghRepo}/${ghBranch}/static/${relativePath}`;
+          console.info(`[Bluesky syndicator] Public URL 404, trying raw GitHub: ${rawUrl}`);
+          mediaResponse = await fetch(rawUrl);
+        }
+      }
 
       if (!mediaResponse.ok) {
         throw await IndiekitError.fromFetch(mediaResponse);
diff --git a/node_modules/@indiekit/syndicator-bluesky/lib/utils.js b/node_modules/@indiekit/syndicator-bluesky/lib/utils.js
index a66dd22..15c4b01 100644
--- a/node_modules/@indiekit/syndicator-bluesky/lib/utils.js
+++ b/node_modules/@indiekit/syndicator-bluesky/lib/utils.js
@@ -49,6 +49,23 @@ export const uriToPostUrl = (profileUrl, uri) => {
   }
 };
 
+/**
+ * Build hashtag string from JF2 category property
+ * @param {object} properties - JF2 properties
+ * @returns {string} Hashtag string, e.g. "#sketch #art"
+ */
+const buildHashtags = (properties) => {
+  const categories = Array.isArray(properties.category)
+    ? properties.category
+    : properties.category
+      ? [properties.category]
+      : [];
+
+  return categories
+    .map((c) => `#${c.replace(/\s+/g, "")}`)
+    .join(" ");
+};
+
 /**
  * Get post test from given JF2 properties
  * @param {object} properties - JF2 properties
@@ -65,7 +82,10 @@ export const getPostText = (properties, includePermalink) => {
     text = htmlToStatusText(properties.content.html);
   }
 
-  // Truncate status if longer than 300 characters
+  const hashtags = buildHashtags(properties);
+  const hashtagSuffix = hashtags ? `\n\n${hashtags}` : "";
+
+  // Truncate status, reserving room for hashtags
   text = brevity.shorten(
     text,
     properties.url,
@@ -73,13 +93,13 @@ export const getPostText = (properties, includePermalink) => {
       ? properties.url
       : false,
     false, // https://indieweb.org/permashortcitation
-    300,
+    300 - hashtagSuffix.length,
   );
 
   // Show permalink below status, not within brackets
   text = text.replace(`(${properties.url})`, `\n\n${properties.url}`);
 
-  return text;
+  return text + hashtagSuffix;
 };
 
 /**
